name: Tests

on:
  pull_request:
    branches: [ develop ]

jobs:
  test:
    name: Test
    runs-on: macOS-latest
    steps:
    - name: Install CocoaPods
      run: pod install
    - run: ls -la
    - name: Tests
      run: fastlane test
